import Button from 'react-bootstrap/Button';

const onClick = ()=>{
  
}

const Create = () => {
  return (
  <Button variant="outline-primary" onClick={onClick}>Create</Button>
  );
}

export default Create